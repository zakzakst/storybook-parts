// https://www.remain-in.com/
import { Other14 } from ".";

import type { Meta, StoryObj } from "@storybook/react-vite";

const meta = {
  title: "Other/14",
  component: Other14,
} satisfies Meta<typeof Other14>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {};
